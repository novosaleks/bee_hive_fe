import styled from 'styled-components';
import { StyledText } from '../../common/style/index';

export const PostInfo = styled(StyledText)`
    a {
        color: inherit;
        text-decoration: none;
    }
`;
